{
  "name": "@shepherd/sdk",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "The public types every extension imports. Pure: no electron, no react, no OS APIs.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc -b",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
