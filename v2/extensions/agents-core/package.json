{
  "name": "@shepherd/ext-agents-core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "shepherd.agents-core \u2014 the vendor-blind agent noun: state, attention, and the kind registry other extensions plug into.",
  "//exports": "Same split as diagnostics: main imports ./manifest to register the extension, the utility process imports the root. ./state is a THIRD subpath because it is the vocabulary a vendor extension type-imports (see claude-code) \u2014 reaching it must not pull in `activate`.",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    },
    "./manifest": {
      "types": "./src/manifest.ts",
      "default": "./src/manifest.ts"
    },
    "./state": {
      "types": "./src/state.ts",
      "default": "./src/state.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc -b",
    "test": "vitest run"
  },
  "dependencies": {
    "@shepherd/sdk": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  },
  "shepherd": {
    "id": "shepherd.agents-core",
    "name": "Agents",
    "version": "0.1.0",
    "api": "^1.0.0",
    "activation": [
      "onStartup"
    ],
    "permissions": [
      "sessions",
      "storage",
      "attention",
      "process.exec",
      "settings"
    ],
    "contributes": {
      "commands": [
        {
          "id": "agents.list",
          "title": "Agents: List Tracked Sessions"
        },
        {
          "id": "agents.resumeTarget",
          "title": "Agents: Resume Target"
        },
        {
          "id": "agents.resumeCommand",
          "title": "Agents: Resume Command"
        },
        {
          "id": "agents.complete",
          "title": "Agents: Ask the Quick Model"
        },
        {
          "id": "agents.quickModel",
          "title": "Agents: Quick Model"
        },
        {
          "id": "agents.quickModelChoices"
        },
        {
          "id": "agents.listModels"
        },
        {
          "id": "agents.defaultModel"
        }
      ],
      "settings": [
        {
          "id": "agents.models",
          "title": "Models",
          "description": "Which model new agents open on, and which one answers the short questions the app asks on your behalf.",
          "order": 100,
          "settings": [
            {
              "key": "agents-core.defaultModel",
              "type": "enum",
              "label": "Default model",
              "group": "New agents",
              "description": "What a new agent opens on. Default lets the agent choose, which is Opus for Claude Code. The composer starts here and can be changed per task.",
              "default": null,
              "nullable": true,
              "choicesFrom": "agents.listModels"
            },
            {
              "key": "agents-core.quickKind",
              "type": "enum",
              "label": "Quick-tier agent",
              "group": "Quick tier",
              "description": "Which agent answers short, non-interactive questions.",
              "default": null,
              "nullable": true,
              "choicesFrom": "agents.quickModelChoices"
            },
            {
              "key": "agents-core.quickModel",
              "type": "enum",
              "label": "Quick-tier model",
              "group": "Quick tier",
              "description": "Left as Default, the chosen agent picks its own.",
              "default": null,
              "nullable": true,
              "choicesFrom": "agents.quickModelChoices"
            }
          ]
        }
      ]
    }
  },
  "//shepherd": "The manifest, as an extension declares it. `src/manifest.ts` is the typed copy the host loads, and `manifest.test.ts` asserts the two are identical \u2014 a built-in is held to the same validation as anybody else."
}
