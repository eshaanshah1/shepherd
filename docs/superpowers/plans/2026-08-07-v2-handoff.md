# v2 handoff — state as of 2026-08-07

**Start here in a fresh session.** Read in this order:
1. [`../specs/2026-08-06-ade-minimal-core-sketch.md`](../specs/2026-08-06-ade-minimal-core-sketch.md) — thesis, primitives, and **every decision** (§7, §7b — including the 2026-08-07 interview: SQLite store, CLI-first no MCP, in-proc React extensions, grant-once permissions, Scratch section, workbench-first post-M4, `.v2` bundle ids, v1 maintenance-only, merge-to-master-early).
2. [`../specs/2026-08-06-ade-v2-core-design.md`](../specs/2026-08-06-ade-v2-core-design.md) — the API + M0–M4 milestones + extension-point primitive + the three-tier session-reading model (`attach()` / `screen()` B-lite / stream-json).
3. [`../specs/2026-08-06-architecture-review.md`](../specs/2026-08-06-architecture-review.md) — v1 debt; its Rebuild checklist is normative.
4. [`../specs/2026-08-06-ade-design-language.md`](../specs/2026-08-06-ade-design-language.md) — **Flock** + the normative token-values table; mock at `2026-08-06-flock-mock.html` (its 8-bit sheep are placeholders — the spec'd sheep are smooth minimal line marks).
5. [`2026-08-07-v2-m0-plan.md`](2026-08-07-v2-m0-plan.md) — the M0 plan the build executed.

## Where the build stands

M0 built by an Opus-executes/Fable-reviews workflow on branch
`fable-electron-refactor`, one commit per phase, each green
(`pnpm -r test` + typecheck + real smokes):

- `ee6dc98` P0 — v2/ pnpm workspace, boundary lint (proven by planted
  violations), node-pty traps closed (pnpm onlyBuiltDependencies +
  spawn-helper chmod), design-tokens with the Flock palette.
- `77bd328` P1 — SplitTree + PtyRing ported tests-first from the Swift
  originals; immutable ops; O(1) ring with a discriminating perf test.
- `b4ae89d` P2 — SessionHost (main process) + typed IPC, coalesced
  (8ms/32KB) Uint8Array batches; preload bridge.
- `6273c13` P3 — React renderer, pane tree from ported SplitTree, Flock
  tokens live; SHEPHERD_CAPTURE self-screenshot.
- `8a09152` P4 — real xterm terminals over the bridge; terminals survive
  React reparenting (v1's remount lesson); menu accelerators settled;
  `shellDefaults()` in platform-darwin.
- P5 (dev/prod isolation + single-instance lock + M0 smoke gate) — in
  flight when this note was written; if its commit is absent, re-run it
  from `2026-08-07-v2-m0-plan.md`'s constraints + the review adjustments
  recorded in the spec docs.

Run it: `cd v2 && pnpm install && pnpm dev` (see v2/package.json scripts;
smokes under `pnpm smoke:*`).

## Next work, in order

1. Finish/verify P5, then **merge this branch to master** (decided: master
   early + often; v2/ touches nothing in spike/).
2. **M1 — the kernel**: extension host (utility process), manifest +
   grant-once permissions, command registry with attributed callers
   (`user|extension|device|agent`), event bus + HTTP-over-unix ingress,
   storage (SQLite), attention aggregation; re-express the app's own chrome
   through the command registry (caller #1). Write the SDK typings against
   the M2/M3 consumers per core-design §5.
3. **M2 — agents** (dogfood-capable), **M3 — tasks** (dogfood-comfortable),
   **M4 — a week of v2-inside-v2** (the gate). Workbench is first after M4.

Process that worked: plan file → Opus feasibility review (no code) → Opus
executes small phases, each committed green with an explicit `deviation`
field → Fable reviews only checkpoints/deviations. Keep it.
